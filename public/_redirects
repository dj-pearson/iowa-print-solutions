# Static assets - serve directly (must be first to avoid catch-all)
/index.html /index.html 200
/IPSLogo-Icon.ico /IPSLogo-Icon.ico 200
/IPSLogo.png /IPSLogo.png 200
/robots.txt /robots.txt 200
/sitemap.xml /sitemap.xml 200
/test-deploy.txt /test-deploy.txt 200
/assets/* /assets/:splat 200

# SPA fallback - all other paths go to index.html (must be last)
/* /index.html 200