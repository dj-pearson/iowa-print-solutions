# HashRouter Configuration - Minimal Redirects Needed
# HashRouter handles client-side routing via URL fragments

# Serve static assets normally
/assets/*  /assets/:splat  200

# Serve static files
/*.ico  /:splat  200  
/*.png  /:splat  200
/*.txt  /:splat  200
/*.xml  /:splat  200

# Only redirect actual missing HTML routes to index.html
/*  /index.html  200