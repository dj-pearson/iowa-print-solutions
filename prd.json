{
  "product": "Iowa Print Solutions",
  "description": "Improvement backlog worked one item per iteration. Items too large for a single iteration are recorded here as stories with the notes needed to resume them. The SEO/GEO backlog in docs/SEO-GEO-STRATEGY.md is the companion document; this file tracks platform and engineering work.",
  "updated": "2026-09-10",
  "stories": [
    {
      "id": "META-1",
      "title": "Remove duplicate SEO meta tags from the shell",
      "status": "done",
      "iteration": 6,
      "problem": "index.html carried description, keywords, robots, canonical, Open Graph and Twitter tags, and SEO.jsx emits all of them through react-helmet. Helmet cannot remove tags it did not create, so every page shipped two of each. All 91 pages declared both their own URL and the site root as canonical; Google disregards conflicting rel=canonical entirely, and a crawler taking the first would fold the site onto the home page.",
      "resolution": "All page-level tags removed from the shell; SEO.jsx owns them. <title> stays as the pre-JS fallback since Helmet replaces the single title element cleanly.",
      "verified": "Three sample routes each carry exactly one description, canonical and og:title, with the canonical matching the page's own URL. Full audit: canonical mismatches fell from 86/87 to 12/87, and the 12 remaining are the pages tracked in SEO-2."
    },
    {
      "id": "FONT-1",
      "title": "Load Inter once, without blocking the first paint",
      "status": "done",
      "iteration": 4,
      "problem": "Inter was requested twice from two different URLs: a <link rel=stylesheet> in index.html (weights 400-700) and an @import at the top of src/index.css (weights 300-700, and nothing on the site uses 300). The @import is the worse of the two - the browser cannot discover it until the CSS bundle has downloaded and parsed, so it serialises an extra round trip. The index.html link was render-blocking, and in a headless Chromium that could not reach Google Fonts the window load event never fired at all.",
      "resolution": "Removed the @import. index.html now uses rel=preload as=style with an onload promotion to stylesheet, plus a noscript fallback. With display=swap the text was going to reflow regardless, so blocking first paint bought nothing.",
      "verified": "Built CSS contains zero fonts.googleapis.com references (was 1). In headless Chromium the load event now fires; before this change page.goto with waitUntil load timed out at 30s on every route."
    },
    {
      "id": "SUSPENSE-1",
      "title": "Infinite render loop in AnalyticsProvider blanked every page",
      "status": "done",
      "estimate": "1-2 iterations",
      "problem": "In a stock headless Chromium, every route on the site shows PageLoadingFallback forever. No h1 renders, document.title stays at the static index.html title, and <main> contains only the skeleton. The React.lazy promise resolves but the Suspense boundary never commits. Reproduces on the production build and the dev server alike.",
      "evidence": [
        "Bisected by adding shell layers one at a time to a two-line dependency-free lazy component: bare React lazy rendered, +ErrorBoundary rendered, +Router rendered, +AnalyticsProvider hung."
      ],
      "iteration": 5,
      "root_cause": "addInteraction was memoised with `score` in its dependency array, while the page-view effect both called addInteraction and listed it as a dependency. Scoring a page view changed the score, which gave addInteraction a new identity, which re-ran the effect, which scored the page view again. The loop never settled.",
      "impact": "Three separate failures from one cycle. The lead score passed 45,000 within a second of page load and climbed about 3,500 a second, writing localStorage every pass. A GA4 lead_scoring event fired on every pass, so the analytics this code exists to collect were being flooded with garbage. And the render loop starved React badly enough that lazy routes never finished suspending, so every page on the site showed the loading skeleton and nothing else - no heading, no content, and the static shell title instead of the page's own.",
      "resolution": "Hold the running score in a ref so addInteraction can read it without depending on it. addInteraction now depends only on location.pathname.",
      "verified": "Before: score 45,709 at t=1s, 59,878 at t=6s, main containing only PageLoadingFallback. After, on both the dev server and the production build: score stable at 2 (the dev figure - StrictMode double-invokes the effect), real h1, the page's own title, aria-busy gone, stable through t=8s."
    },
    {
      "id": "PWA-1",
      "title": "Repair PWA install and offline support",
      "status": "done",
      "iteration": 1,
      "problem": "public/sw.js precached /favicon.ico and /IPS-blue.webp, neither of which ships. cache.addAll() is atomic, so install() rejected on every visit and the service worker never activated - the site had no offline support at all. manifest.json separately declared one 256x256 PNG as both 192x192 and 512x512, which fails Chrome's installability check, and marked it 'any maskable' with no safe-zone padding.",
      "resolution": "Generated real 192/512 icons plus a padded maskable 512 and a 180 apple-touch icon into public/icons/. Rewrote manifest icons with matching sizes and split any/maskable purposes. sw.js now precaches individually via Promise.allSettled so one bad path degrades one asset instead of the whole worker, cache bumped to v2, navigation fallback serves the cached shell before the offline page. Registration gated to import.meta.env.PROD so the dev server stops serving stale bundles.",
      "verified": "npm run build succeeds; every path in STATIC_ASSETS resolves to a file in dist/."
    },
    {
      "id": "SEC-1",
      "title": "Baseline security response headers",
      "status": "done",
      "iteration": 3,
      "problem": "public/_headers carried only X-Frame-Options, X-Content-Type-Options and Referrer-Policy. No HSTS, no Permissions-Policy, no CSP of any kind. Separately, hashed assets were cached for a year without 'immutable' so browsers still revalidated on reload, and unhashed files served from the site root (/icons/*, /IPSLogo.png) had no Cache-Control at all.",
      "resolution": "Added Strict-Transport-Security (one year, apex only - includeSubDomains and preload are the domain owner's call once every subdomain is known HTTPS-only), Content-Security-Policy: frame-ancestors 'none', a deny-everything Permissions-Policy, and Cross-Origin-Opener-Policy: same-origin. Hashed /assets/* now immutable; unhashed statics get a revalidatable week; crawler files and the SPA shell stay short-lived.",
      "verified": "_headers is syntactically a Cloudflare Pages rules file; every path pattern corresponds to a real file or directory in dist/.",
      "open": "Add includeSubDomains, then preload, once the subdomain inventory is confirmed HTTPS-only."
    },
    {
      "id": "RENDER-1",
      "title": "Pre-render routes to static HTML",
      "status": "todo",
      "estimate": "3+ iterations",
      "problem": "The site is a client-rendered SPA - dist/index.html ships an empty <div id=\"root\">. Google renders JS on a delay and a budget; several AI answer crawlers do not execute JS at all and see nothing. This is item 1 on the SEO/GEO backlog and the single largest remaining technical win.",
      "notes": [
        "94 routes in src/App.jsx, all React.lazy. scripts/generate-sitemap.mjs already parses that route table - reuse its extraction rather than writing a second parser.",
        "react-helmet (not helmet-async) is in use. It has no server-side collection story that works cleanly with concurrent React 18; a headless-browser snapshot avoids the problem entirely because Helmet has already written the real <head>.",
        "Cloudflare Pages runs the build, so the prerender step has to work in that container. Playwright/Puppeteer downloading Chromium at build time is the risk to settle first: measure install time and size, and have a fallback of prerendering only high-value routes.",
        "This environment has Chromium at /opt/pw-browsers with PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1, so local prototyping is free - do not mistake that for it working on Pages.",
        "Split the work: (1) prototype a postbuild snapshot script locally and confirm rendered HTML contains real headings, meta and schema; (2) settle the Cloudflare build story; (3) wire it into the build and verify hydration does not warn."
      ]
    },
    {
      "id": "LINT-1",
      "title": "Make npm run lint usable as a gate",
      "status": "done",
      "estimate": "2+ iterations",
      "notes": [
        "The 24 remaining are left as genuine signal, not silenced - see CONTENT-1. Adding lint to the build should wait until they are resolved.",
        "Both exhaustive-deps fixes matter given SUSPENSE-1: Breadcrumbs rebuilt a static label map on every render, so it could never be a useMemo dependency without defeating the memo (hoisted to module scope); Navbar read megaMenuRef.current in an effect cleanup, so the keydown listener could be added to one node and removed from another (node captured in effect scope, and the focus timer is now cleared)."
      ],
      "iteration": 13,
      "resolution": "794 errors down to 24, with the build passing at every step.\nRemoved 142 unused import specifiers across 127 files, driven by ESLint's own report rather than by guessing - 121 of them were React default imports left over from before the automatic JSX runtime.\nTurned off react/prop-types (302 of the 794): a JavaScript-only marketing site with no TypeScript and no shared component library gets little from it, and the volume was hiding real findings. Narrowed react/no-unescaped-entities to forbid only > and } rather than disabling it; essentially all 192 reports were apostrophes in ordinary prose.\nFixed every genuinely real error: a duplicate break in PrintCostCalculator, process.env in browser code (now import.meta.env.DEV - it only worked because vite.config.js happens to define that one key), fetchpriority miscased for React, four no-op regex escapes in the phone validator, two unexplained empty catch blocks, and both react-hooks/exhaustive-deps warnings.",
      "verified": "npm run eslint reports 24 errors and 4 warnings, from 794 errors and 6 warnings. npm run build succeeds."
    },
    {
      "id": "CSP-1",
      "title": "Ship a real Content-Security-Policy",
      "status": "todo",
      "estimate": "2+ iterations",
      "problem": "SEC-1 added only frame-ancestors, which cannot break rendering. A policy covering script-src, style-src, img-src and connect-src is the actual protection, and it is the one header on this site that will break pages if it is wrong.",
      "notes": [
        "index.html has an inline gtag bootstrap, so script-src needs a nonce or a hash. Cloudflare Pages serves static files and cannot inject a per-request nonce - a build-time hash of the inline block is the workable route.",
        "Origins to allow, taken from an audit of index.html and src/: googletagmanager.com and google-analytics.com (script, connect), fonts.googleapis.com (style) and fonts.gstatic.com (font), images.unsplash.com (img), calendly.com (frame, if the widget is embedded rather than linked).",
        "framer-motion writes inline styles, so style-src almost certainly needs 'unsafe-inline'. Confirm before assuming it can be tightened.",
        "Ship it as Content-Security-Policy-Report-Only first and watch for violations before enforcing. Do not enforce and verify in the same iteration.",
        "Verify by loading the built site under a headless browser and asserting zero CSP violations in the console, not by reading the policy."
      ]
    },
    {
      "id": "IMG-1",
      "title": "Stop hotlinking hero images from Unsplash",
      "status": "todo",
      "estimate": "2+ iterations",
      "problem": "45 references to images.unsplash.com across src/. Page-critical imagery is served by a third party the site does not control: it is an extra DNS and TLS handshake in the LCP path, the URLs can change or 404, and it hands a third party a request log of every visitor.",
      "notes": [
        "Check each image's Unsplash licence attribution requirement before self-hosting.",
        "src/components/ImageOptimizer.jsx is 130 lines with zero importers. It handles lazy loading, fetchpriority and sizes - exactly this job - and is dead code. Decide whether to wire it into Blog.jsx or delete it; leaving it implies images are optimised when nothing uses it."
      ],
      "priority": "low",
      "correction": "Also overstated. All 45 images.unsplash.com references are in one file, src/pages/Blog.jsx, as the blog index card images. They already have width=400 height=250 and loading=lazy, and they sit below the fold on a single route - not 'in the LCP path' across the site as originally written. Still worth self-hosting for the third-party dependency and the visitor request log, but it is not urgent."
    },
    {
      "id": "REPO-1",
      "title": "Stop tracking dist/ in git",
      "status": "done",
      "estimate": "1 iteration",
      "problem": "dist/ is committed. Every build rewrites ~80 content-hashed chunk filenames, so an unrelated one-line source change produces a 160-file diff of deletions and additions that buries the real change in review.",
      "iteration": 11,
      "resolution": "git rm -r --cached dist, and .gitignore now ignores it with a comment explaining why the entry had been commented out and why that reason no longer holds.",
      "verified": "Confirmed Cloudflare Pages builds from source before removing anything: CLOUDFLARE-DEPLOYMENT.md records build command `npm run build` and output directory `dist`, there are no GitHub workflows in the repo at all, and the Cloudflare Worker the gitignore comment cited is disabled (worker.js.disabled, wrangler.toml.disabled). Deleted dist/ entirely and rebuilt from clean: 0 files tracked, git check-ignore confirms it is ignored, `wrangler pages deploy` still has a local dist to upload."
    },
    {
      "id": "SEO-2",
      "title": "Twelve resource guides render no SEO tags at all",
      "status": "done",
      "estimate": "1 iteration",
      "problem": "12 of the 18 pages under /resources import no SEO component. They have no title, no meta description and no canonical, so they all share the shell title and go to search engines with nothing describing them. These are keyword-targeted guides - it is the one page type where this matters most.",
      "notes": [
        "No keywords prop was passed. SEO.jsx falls back to the site defaults, and meta keywords are slated for removal anyway (backlog item 9 in docs/SEO-GEO-STRATEGY.md)."
      ],
      "iteration": 8,
      "resolution": "Added an SEO component to each of the 12, with a title, a description written from that page's own content, a canonical, and schemaType TechArticle. Titles are sized against the 23-character ' | Iowa Print Solutions' suffix that getPageTitle appends, so all 12 land at 44-56 characters rendered. Descriptions are 144-156 characters and all distinct. The PrinterLogic pages name Vasion Print first with the old name after, per the naming rule in CLAUDE.md; the URLs are unchanged.",
      "verified": "All 12 routes on the production build: exactly one canonical and one description each, canonical matches the route, title under 60 characters, description 100-160 characters, 12 unique descriptions out of 12."
    },
    {
      "id": "RENDER-2",
      "title": "Four pages crashed into the ErrorBoundary",
      "status": "done",
      "estimate": "1 iteration",
      "problem": "/blog/papercut-hive-q4-2025-iowa, /industries/iowa-education-print-solutions, /industries/iowa-healthcare-print-solutions and /services/printerlogic-iowa time out waiting 20s for an h1 or h2 in <main>. Every other route renders in under a second. This is separate from SUSPENSE-1, which is fixed - these four still fail.",
      "iteration": 7,
      "root_cause": "Three distinct bugs, all of the same class. PrinterLogicIowa used the lucide icon Cloud and PaperCutHiveQ42025Iowa used DollarSign without importing either. Both industry pages passed a JSX element to ServiceCard's icon prop, which destructures `icon: Icon` and renders <Icon />, so it needs a component reference. The healthcare page additionally used SmartLeadCTA without importing it, passing defaultCTA/urgentCTA/premiumCTA props that the component does not implement - it takes children, variant and size.",
      "resolution": "Added the two missing icon imports. Changed the four ServiceCard icon props to component references, leaving the separate icon arrays as elements since those are consumed directly as {service.icon}. Rewrote the healthcare CTA section to use SmartLeadCTA as it is actually built, with real links and the real phone number from siteConfig.",
      "verified": "All four routes now render a real h1 and 7-8 h2 sections with no pageerror, on the production build.",
      "follow_up": "react/jsx-no-undef is already an error in eslint.config.js - it would have caught two of these. It was invisible among the ~795 existing errors. See LINT-1."
    },
    {
      "id": "CLS-1",
      "title": "Logo images had no intrinsic size (and the CLS framing was overstated)",
      "status": "done",
      "estimate": "1-2 iterations",
      "problem": "Two thirds of the images on the site declare no intrinsic size, so the browser cannot reserve space before they load and the layout shifts. Cumulative Layout Shift is a Core Web Vital.",
      "iteration": 12,
      "correction": "The original story read '89 of 134 images have no width or height' as if the site were full of shifting imagery. Source has five <img> tags in total. The 134 count is one shared Navbar logo rendered on 87 pages plus a handful of others, and the 89 undimensioned ones are that Navbar logo plus two in Hero. All three set both dimensions in CSS (h-8 w-8, h-20 w-20), so the browser reserves the box as soon as the stylesheet applies and real-world shift is close to zero. The blog card images already carried width, height and loading=lazy.",
      "resolution": "Added matching width/height attributes to the three logo tags. Correct and cheap, but it buys a fraction of a layout shift before CSS parses, not a Core Web Vitals win."
    },
    {
      "id": "TITLE-1",
      "title": "Titles and descriptions overflow their SERP limits",
      "status": "todo",
      "estimate": "1-2 iterations",
      "problem": "86 of 87 page titles exceed 60 characters and 65 descriptions exceed 160, so Google truncates them mid-phrase. The call to action ends up cut off.",
      "notes": [
        "SEO.jsx appends a site-name suffix to every title - measure the final rendered length, not the string passed in.",
        "This is worth doing after SEO-2 so the 12 new pages are written to the limit rather than fixed twice.",
        "The 12 resource guides from SEO-2 are already within limits and can be skipped."
      ]
    },
    {
      "id": "HEAD-1",
      "title": "Heading levels skipped on 21 routes",
      "status": "done",
      "estimate": "1 iteration",
      "problem": "21 routes jump straight from h1 to h3. Screen reader users navigate by heading level, and a skipped level reads as a missing section.",
      "notes": [
        "Not one shared component, which was the initial guess - the jumps came from three unrelated patterns.",
        "The h4s nested under a real h3 in the same files were deliberately left alone; a blanket h4-to-h3 pass would have broken them."
      ],
      "iteration": 10,
      "resolution": "Two layers. The visible one was a lead-in callout - 'Why this matters', 'Key Takeaways', 'Executive Summary' - marked h3 directly under the h1 on 19 pages; those became h2. Fixing them exposed a second layer the scanner had been hiding, because it only reports the first jump per page: 12 blog posts put an h4 checklist directly under an h2 section, skipping h3. Those became h3. Two h2->h4 cases were the same shape.\nOne case was not a level problem at all. The hero card on the home page labelled a decorative product panel with an h3. Promoting it to h2 would have claimed a page section that does not exist, so it is a <p> with the same type scale.",
      "verified": "Browser scan of all 91 routes: 0 heading jumps, down from 21. Every change rewrites the tag only; class names are untouched, so nothing moves visually."
    },
    {
      "id": "CTA-1",
      "title": "SmartLeadCTA discarded its callers' copy; templates imported from paths that did not exist",
      "status": "done",
      "estimate": "1 iteration",
      "problem": "SmartLeadCTA accepts children, className, variant and size. EnhancedIndustryTemplate and EnhancedLocationTemplate call it with defaultCTA, urgentCTA and premiumCTA objects, which are silently discarded - the component renders the string 'Learn More'. The same call shape was on the healthcare page and is now fixed there.",
      "iteration": 9,
      "resolution": "Implemented the defaultCTA/urgentCTA/premiumCTA API in SmartLeadCTA rather than stripping it from the callers - three call sites agreed on that shape, so the callers were right about what the component is for. Which tier renders is chosen from the visitor's lead grade via the real useLeadScoring in AnalyticsProvider; missing tiers fall back to defaultCTA. The children call shape still works. Removed the duplicate stub useLeadScoring that shadowed the real hook by name. Fixed every relative import in both page templates and replaced the invented (515) 123-4567 with siteConfig.business.phoneDisplay and phone.",
      "verified": "/blog rendered a blue box reading 'Learn More' where its CTA copy should have been; it now renders the real CTA with the correct phone number. No occurrence of 123-4567 remains in src/. Every relative import in both templates resolves to a file on disk."
    },
    {
      "id": "CONFIG-1",
      "title": "Tailwind Typography plugin was installed but never registered",
      "status": "done",
      "iteration": 12,
      "problem": "tailwind.config.js had plugins: []. Five pages - the privacy policy, the accessibility statement and three blog posts - use prose, prose-lg and prose-blue. None of those classes generated any CSS: the built stylesheet contained zero .prose rules, so those pages rendered as unstyled walls of text with no paragraph spacing, list markers or heading rhythm. @tailwindcss/typography was in package.json and named in CLAUDE.md's tech stack the whole time.\nThe repo also carried tailwind.config.cjs and postcss.config.cjs alongside the .js versions. The .cjs Tailwind config defines a different primary colour (#1A237E against the .js file's #2563eb blue ramp), so which file wins silently decides the site's palette.",
      "resolution": "Registered the plugin as an ESM import, matching the config's export default. Deleted both .cjs duplicates after confirming neither postcss-import nor tailwindcss/nesting is used - src/index.css has no @import and no nesting.",
      "verified": "Built stylesheet went from 0 .prose rules at 63,512 bytes to 151 rules at 82,103 bytes, with existing utilities intact. In a browser, a ul inside .prose now computes list-style-type disc with 28px padding, where Tailwind preflight resets lists to none and 0 - so the plugin is demonstrably applying."
    },
    {
      "id": "CONTENT-1",
      "title": "Written content on eight resource guides is never rendered",
      "status": "todo",
      "estimate": "1-2 iterations",
      "problem": "16 of the 24 remaining lint errors are large content arrays that are defined and never used: implementationSteps, troubleshooting, securityBenefits, complianceFeatures, implementationPhases, businessBenefits, technologyComponents. PaperCutMobilityPrint's implementationSteps alone is an 89-line four-phase plan with durations and per-phase activities. Someone wrote this material and it reaches no visitor.",
      "affected": [
        "src/pages/resources/PaperCutFollowMePrint.jsx",
        "src/pages/resources/PaperCutMobilityPrint.jsx",
        "src/pages/resources/PaperCutSecurePrintRelease.jsx",
        "src/pages/resources/UniflowCostTracking.jsx",
        "src/pages/resources/UniflowFlexibleAccessControl.jsx",
        "src/pages/resources/UniflowMobileCloudPrinting.jsx",
        "src/pages/resources/UniflowPrintFromAnywhere.jsx",
        "src/pages/resources/UniflowSecurePrintRelease.jsx"
      ],
      "notes": [
        "Read each array before deciding. Some of it may be the unverified product claims docs/SEO-GEO-STRATEGY.md warns about, in which case it should go rather than ship.",
        "Anything with a duration, a percentage or a version number needs a source before it is rendered - the standing rules in the strategy doc apply.",
        "The other 8 errors are unused function parameters (index, contentId, title, location). Prefix with _ and set argsIgnorePattern, or use them.",
        "Once this is closed, lint is clean and can be added to the build."
      ]
    }
  ]
}